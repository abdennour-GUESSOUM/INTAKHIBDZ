class DeputiesWinnerModel {
  String? groupAddr;
  BigInt? votes;
  String? groupName;
  String? pictureUrl;
  double? percentage;

  DeputiesWinnerModel(this.groupAddr, this.votes, this.groupName, this.pictureUrl, this.percentage);
}
