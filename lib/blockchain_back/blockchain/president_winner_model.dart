class PresidentWinnerModel {
  String? address;
  BigInt? votes;
  String? firstName;
  String? lastName;
  String? imageUrl;
  double? percentage;

  PresidentWinnerModel(this.address, this.votes, this.firstName, this.lastName, this.imageUrl, this.percentage);
}
